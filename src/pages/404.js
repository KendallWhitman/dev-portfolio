import React from "react"

import Layout from "../components/layout"

const NotFoundPage = () => (
  <Layout>
    <section className="section section--404">
      <h1 className='section__title'>Not Found</h1>
      <h2 className='section__text'>Sorry, it's just the one page :)</h2>
    </section>
  </Layout>
)

export default NotFoundPage
